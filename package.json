{
  "name": "plebly-proposals",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-proposal.mjs",
    "validate:all": "node scripts/validate-proposal.mjs --all",
    "check:fees": "node scripts/check-fee-payments.mjs",
    "test": "node --test scripts/check-fee-payments.test.mjs scripts/validate-schema-fields.test.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "gray-matter": "^4.0.3",
    "yaml": "^2.7.0"
  }
}
